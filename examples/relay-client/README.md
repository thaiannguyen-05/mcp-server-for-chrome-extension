# Relay Client Example

This directory will contain an example client that demonstrates how to call `tools/list` and `tools/call` on the MCP extension server.

## TODO

- Implement basic client that connects to extension
- Example calls to tools/list
- Example calls to tools/call
